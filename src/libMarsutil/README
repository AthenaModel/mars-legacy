Paxutil: JNEM util(n) Binary Tcl Extensions
-----------------------------------------------------------------

libPaxutil is a binary Tcl extension component of the util(n) 
package.  Some few of the commands are provided only in binary
form; most are efficient binary implementations of commands
otherwise implemented in Tcl.

