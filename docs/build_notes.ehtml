<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN">
<html>
<head>
<title>Mars <<version>> Simulation Infrastructure Library</title>
</head>

<body bgcolor="#F5F5F5" alink="Blue" link="Blue">
<h1 style="background: red;">&nbsp;Mars <<version>>: Simulation Infrastructure
Library</h1>

<h2>Build Notes</h2>

<h3>Build 1.12, 27 May 2009</h3>

This build contains a fair number of changes, most of them in support
of ongoing Athena development; a few relate to bugs found during
integration of JNEM 4.1.<p>

<ul>
  <li> Bug 2048: simlib(n): Add coverage(n) coverage function module
  <li> Bug 2053: gram(n): error on initialization
  <li> Bug 2072: sqlib(n)/sqldocument(n): replace
  <li> Bug 2074: eventq(n): Can't schedule events defined before "eventq init"
  <li> Bug 2087: eventq(n): checkpoint/restore is broken
  <li> Bug 2112: jnem_sim(1): SPR 1529 - JNEM throws bgerrors on time advance.
  <li> Bug 2114: bgerror: simclock(n), rtiproxy(n): advance request when advance is pending 
</ul>


<h3>Build 1.11, 13 April 2009</h3>

There are three changes in support of ongoing Athena development in
this build:<p>

<ul>
  <li> Bug 1978: parmset(n): add saveable(i) interface
  <li> Bug 1985: gram(n): clear
  <li> Bug 2036: gram(n): slope inputs no longer have "limit" 
</ul><p>

<h3>Build 1.10, 25 March 2009</h3>
Four changes in support of ongoing Athena development are in this build. There's
also a patch to prevent a bgerror from occurring in log browsers.

<ul>
  <li>Bug 1954: simlib(n): simtypes(n), rmf(n), gram(n)
  <li>Bug 1956: sqldocument(n): -clock option
  <li>Bug 1957: zulu(n): zulu validate
  <li>Bug 1960: saveable(i) checkpoint -saved
</ul>

<h3>Build 1.9, 11 March 2009</h3>
Two minor changes in this build. marsutil(n) no longer includes the parmdb(n)
man page and Mars now uses the 2008Q4 version of ActiveTcl Enterprise Edition.<p>

<ul>
  <li> Bug 1917: marsutil(n) erroneously includes parmdb(n) man page
  <li> Bug 1928: Use ATEE 2008Q4
</ul>

<h3>Build 1.8, 11 February 2009</h3>

This build has two minor changes.  The range(n) type now participates
in the "INVALID" error protocol, and sqldocument(n) now provides the
ability to insert a dictionary of column names and values into a
table.<p>

<ul>
  <li> Bug 1898: sqlib(n), sqldocument(n): insert dict into table
  <li> Bug 1902: range(n): -errorcode INVALID 
</ul><p>

<h3>Build 1.7, 6 February 2009</h3>

This build has two minor changes: The removal of an unused SQL table and an 
increase in the precision of MGRS locations from 100m resolution to 1m
resolution. The following packages are included:<p>

<ul>
  <li>Bug 1889: sqldocument(n): remove sqldocument_master
  <li>Bug 1894: MGRS locations should have 10 digits
</ul>

<h3>Build 1.6, 27 January 2009</h3>
This build includes some new functionality for loglist(n), filter(n), 
enum(n) and quality(n). There are also some icons added for the CV tool.
The following packages are included:
<ul>
  <li>Bug 1852: loglist(n): make applist optional
  <li>Bug 1855: Need to add/modify icons in support of CV tool
  <li>Bug 1865: mars_link(1): "mars link save" doesn't commit the change.
  <li>Bug 1867: enum(n), quality(n): Better HTML output
  <li>Bug 1876: filter(n): Incremental and case-insensitive filtration
</ul>

<h3>Build 1.5, 9 January 2009</h3>
This build includes some minor enhancements and a bug fix. Zcurves are now
more flexible in their use. The following packages are included.
<ul>
   <li>Bug 1754: mars_man(1): Stack trace on unknown parent module
   <li>Bug 1815: Add a -cmd option to indicate the start of a refresh
   <li>Bug 1825: zcurve(n): Add instances with min/max limits
</ul>

<h3>Build 1.4, 3 December 2008</h3>
This build includes some new functionality in a few areas: sequence diagrams,
introduction of pixane, a mars commit tool, and a few others in the bugs list
below. There are also a couple of bug fixes. The following packages are 
included:
<ul>
   <li>Bug 1783: pixane(n): New module in mars.
   <li>Bug 1787: sequence(5) sequence diagram format
   <li>Bug 1789: Log level pull-downs in consoles are too narrow to display
   <li>Bug 1790: sequence(5) sequence diagram format, Part 2
   <li>Bug 1791: sequence(n): Fix for pixane text handling
   <li>Bug 1794: geometry(n): boxaround
   <li>Bug 1796: marsmisc(n): normalize
   <li>Bug 1800: sequence(n): Draw dots at tail of message arrows
   <li>Bug 1801: sequence(n): message text alignment
   <li>Bug 1803: mars_commit(1): New tool
   <li>Bug 1804: sequence(n): diagrams with titles cause a stack trace
   <li>Bug 1805: sqldocument(n): nonempty() function
   <li>Bug 1806: notifier(n): trace
   <li>Bug 1808: marsgui(n): -setgrid
   <li>Bug 1811: geoset(n): delete
</ul>

<h3>Build 1.3, 14 November 2008</h3>
This build includes some updates to sqldocument to make it more flexible. It
also introduces the saveable(i) interface, which is used by cli(n). Some bug
fixes and man pages are also part of this build. The following packages are
included:
<ul>
  <li>Bug 1439:	sqldocument(n) saveas fails when called for jnem_sim(1)'s RDB
  <li>Bug 1697:	sqldocument(n): need a read-only option to open the database
  <li>Bug 1765:	sqldocument(n): -autotrans, -rollback, saveas
  <li>Bug 1767:	mars_man(5): Man page sections
  <li>Bug 1768:	marsgui(n): Remove dependency on Img
  <li>Bug 1770:	jnem_log(1): Disabling scroll lock doesn't update the log.
  <li>Bug 1774:	saveable(i), cli(n): Saveable Interface
  <li>Bug 1775:	logger(n): -newlogcmd
  <li>Bug 1778:	marsmisc(n): "try" not exported
</ul>

<h3>Build 1.2, 06 November 2008</h3>
This build includes more migration from JNEM to Mars and a few bug fixes. 
There is also the introduction of a couple of new commands.
The following packages are included in this build:
<ul>
  <li>Bug 1727: JNEM uses Martian infrastructure, Part II
  <li>Bug 1728: mars_link(1): replacement for mars_import(1)
  <li>Bug 1731: ehtml(n) supports "root:name(section)" syntax for man pages.
  <li>Bug 1734: Update JNEM to use mars_man(1) to process man pages.
  <li>Bug 1741: Migrate util(n) to marsutil(n)
  <li>Bug 1742: Migrate gui(n) to Mars as marsgui(n)
  <li>Bug 1753: mars(1): Error in application list output
  <li>Bug 1756: mkicon(n): Tk Icon Creation Utility
  <li>Bug 1759: geometry(n): cedge
  <li>Bug 1765: sqlib(n): saveas now commits changes to the database being saved
  <li>Bug 1766: Log level fine-tuning
</ul>

<h3>Build 1.1, 15 October 2008</h3>
This is the first Mars build. It introduces Mars as a separate library that can
be used by multiple applications.

</body>
</html>
